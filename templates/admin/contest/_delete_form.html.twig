<form method="post" action="{{ path('app_admin_contest_delete', {'id': contest.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contest.id) }}">
    <button class="btn">Delete</button>
</form>
