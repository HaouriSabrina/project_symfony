{% extends 'base.html.twig' %}

{% block title %}Hello TestController!{% endblock %}
{% block piedpage %} Fichier index.html.twig {% endblock %}
{% block neuf %} Ceci ne s'affichera pas{% endblock %}
{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    {# "double {{}} remplace la balise echo du php <?= ?>" #}
    {# Les variables en twig ne sont pas devancées par un caractère $ contrairement au php  #}

    <p> {{texte}} </p>
    {# Les test twig : 
        Syntaxe : variable is test
        Parmi les test il y a defined, null, empty,....

        la balise TWIF IF : 
        SYNTAXE : 
        {% if condition %}
        {% elseif condition %}
        ...
        {% else %}
        {% endif %}

        https://twig.symfony.com/doc/3.x/

    #}
    {% if calcul is defined %}
        <p> Le résultat du calcul est : {{calcul}} </p>
    {% endif %}
    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/xampp/htdocs/myscoreboard/src/Controller/TestController.php'|file_link(0) }}">src/Controller/TestController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/xampp/htdocs/myscoreboard/templates/test/index.html.twig'|file_link(0) }}">templates/test/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %}
